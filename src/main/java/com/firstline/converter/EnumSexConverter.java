package com.firstline.converter;

/*
@Converter(autoApply = true)
public class EnumSexConverter implements AttributeConverter<Sex, Integer> {
    @Override
    public Integer convertToDatabaseColumn(Sex attribute) {
        return attribute.getId();
    }

    @Override
    public Sex convertToEntityAttribute(Integer dbData) {
        return Sex.valueOf(dbData);
    }
}
*/
